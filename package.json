{
  "dependencies": {
    "firebase": "^12.1.0"
  },
  "scripts": {
    "emulators": "firebase emulators:start --import=.emulator-data --export-on-exit",
    "dev:local": "concurrently -k -n emu,web -c blue,green \"npm run emulators\" \"npm --prefix apps/web run dev:emu\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0",
    "firebase-tools": "^13.16.1"
  }
}
